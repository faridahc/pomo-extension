# pomo-extension
Pomo is a minimal Pomodoro timer Chrome extension built with Manifest V3. It runs a persistent background timer that survives tab switches and browser restarts, sends desktop notifications when sessions end, and tracks your 4-session focus cycle. Built with vanilla HTML, CSS, and JavaScript — no frameworks, no dependencies.
